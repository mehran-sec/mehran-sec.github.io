# Projects collection

This folder is the `_projects` collection (configured in `_config.yml`).
It's intentionally empty right now — real projects go here as `.md` files
once they exist, using `_includes/project-card.html` for the front-matter
schema. No placeholder/fake projects are added here on purpose.
